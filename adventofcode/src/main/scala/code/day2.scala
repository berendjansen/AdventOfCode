import scala.io.Source

object day2 {
  def main(args: Array[String]): Unit = {
    // val data = Source.fromFile("/data/day1example.txt").mkString
    // println(data)
    var currentDirectory = System.getProperty("user.dir");
    print(currentDirectory)
  }
}
